<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/darkBlue</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:textCursorDrawable">@null</item>
        <item name="colorControlNormal">@color/darkBlue</item>
        <item name="colorControlActivated">@color/darkBlue</item>
        <item name="colorControlHighlight">@color/darkBlue</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:scaleType">fitXY</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="pinkButton" parent="Theme.AppCompat.Light.NoActionBar" tools:targetApi="21">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/lightBlue</item>
        <item name="android:textColor">#FFF</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:textCursorDrawable">@null</item>
        <item name="colorControlNormal">@color/pink</item>
        <item name="colorControlActivated">@color/darkPink</item>
        <item name="colorControlHighlight">@color/darkPink</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:scaleType">fitXY</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:colorButtonNormal">@color/pink</item>
    </style>

    <style name="darkButton" parent="Theme.AppCompat.Light.NoActionBar" tools:targetApi="21">
        <!-- Customize your theme here. -->
        <item name="android:background">@color/lightBlue</item>
        <item name="android:textColor">#FFF</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:textCursorDrawable">@null</item>
        <item name="colorControlNormal">@color/blue</item>
        <item name="colorControlActivated">@color/darkBlue</item>
        <item name="colorControlHighlight">@color/darkBlue</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:scaleType">fitXY</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:colorButtonNormal">@color/blue</item>
    </style>

</resources>
